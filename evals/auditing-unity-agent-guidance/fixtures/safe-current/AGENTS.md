# Agent Instructions

Resolve the exact Unity project-copy path before automation. Inspect it with `unity_project_status`. Prefer connected Pipeline commands only when that exact Editor is reachable and advertises the command; otherwise use an isolated project-approved route. Never install packages, close Editors, or delete lockfiles implicitly.
