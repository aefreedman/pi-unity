# Coordination guidance

This file applies to all workspace copies.

- Use headless Unity open/compile as the mandatory compile check.
- Run `Unity.exe -batchmode -projectPath <workspace>/Game -quit -logFile compile.log`.
