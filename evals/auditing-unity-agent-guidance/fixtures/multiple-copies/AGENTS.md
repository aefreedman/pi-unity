# Unity Automation

There are two copies at `ws1/Game` and `ws2/Game`.

Use `unity command recompile` to compile whichever Editor is open. If that fails, run `unity -batchmode -projectPath Game -quit`.

Never launch more than one Unity process at a time.
