# unity-docs eval seeds

Behavioral seeds for solution-capture provenance and migration authorization. Deterministic schema, collision, journal, backup, link, and recovery behavior is covered by `npm test`; live model trials remain opt-in.
